# Group 3 Geothermal FEFLOW Automation
# Python >= 3.9
#
# FEFLOW 8.1 must be installed separately — it is NOT on PyPI.
# The ``ifm`` module (ifm312.pyd) ships with FEFLOW and is imported
# automatically when scripts are run from FEFLOW's bundled Python interpreter.
#
# Optional richer API wrapper (pandas integration):
#   pip install ifm_contrib     # github: dhi/ifm_contrib

# Numerics
numpy>=1.24

# Data I/O
pandas>=2.0
openpyxl>=3.1       # .xlsx read/write (required by pandas.read_excel)

# Visualisation
matplotlib>=3.7

# Testing (development only)
pytest>=7.4
